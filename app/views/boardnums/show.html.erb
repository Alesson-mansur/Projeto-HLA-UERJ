<%- model_class = Boardnum -%>
<div class="page-header">
  <h1>Detalhes do Cadastro</h1>
</div>

<dl class="dl-horizontal">
  <dt><h4><strong>Número da Placa:</strong></h4></dt>
  <dd><h4><%= @boardnum.identification %></h4></dd>
</dl>

<%= link_to t('.back', :default => t("helpers.links.back")),
              boardnums_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_boardnum_path(@boardnum), :class => 'btn btn-info' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              boardnum_path(@boardnum),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
