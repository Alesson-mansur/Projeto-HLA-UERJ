<%= form_for @boardnum, :html => { :class => "form-horizontal boardnum" } do |f| %>

  <% if @boardnum.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@boardnum.errors.count, "error") %> prohibited this boardnum from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @boardnum.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <div class="control-label col-lg-3">
      <b><h4>Número da placa:</h4></b>
    </div>
    <div class="col-lg-4">
      <%= f.text_field :identification, :class => 'form-control' %>
    </div>
    <%=f.error_span(:identification) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit "Enviar", :class => 'btn btn-primary' %>
      <%= link_to 'Cancelar', boardnums_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
